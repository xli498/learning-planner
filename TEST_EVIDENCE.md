# 学习计划助手静态包｜测试证据

测试时间：2026-08-10（开发者实测）  
范围：`index.html`，本地静态打开；数据仅为虚构示例。  
限制：未做真实用户走查；不得将本记录表述为用户可用性结论。

## 功能实测记录

| 用例 | 操作与预期 | 结果 | 证据 |
|---|---|---|---|
| AT-01 / AT-02 | 保存目标日期、每周可用小时、高效时段；不填写时保持为空，不生成伪精确计划 | 通过 | 表单将空值保留为 0 / 未填写，任务仅由用户手动添加 |
| AT-04 / AT-05 / AT-06 | 同科添加主资料后再添加主资料；添加辅助/暂停资料并生成任务 | 通过 | 第二主资料被阻止；暂停资料不可选入任务；资料角色文字标签可见 |
| AT-07 | 添加高/中/低优先级任务并打开“现在做” | 通过 | 按优先级和日期排序，仅突出一项；任务展示科目、资料、时长、完成量、步骤、复盘、优先级 |
| AT-08 | 点击完成、在 5 秒内撤销 | 通过 | 完成状态更新；撤销恢复完成前状态；超时显示撤销窗口结束 |
| AT-09 | 无任务进入今日区 | 通过 | 显示创建任务/补充可用时间提示 |
| AT-10~14 | 对任务点延期/卡住，分别选择原因、空原因、三种调整方式；先预览，再确认 | 通过 | 原因记录；空原因明确为信息不足；预览展示受影响任务数、用户填写日期、资料不变；确认后才修改任务并新增版本 |
| AT-12 / AT-22 | 调整页直接关闭，再重新进入并确认调整 | 通过 | 关闭不改计划；确认后新增 `confirmed-replan` 版本，显示时间、原因、变更和影响数 |
| AT-21 | 检查隐私文案、本地保存、导出和导入确认 | 通过 | 明示 localStorage 与清除风险；导入前浏览器确认；无网络请求/账号字段 |
| 目标切换 | 创建“英语听力”任务后填写国考目标并切换 | 通过 | 浏览器确认框预览科目/资料/任务数量；确认后全部清空，国考中无英语听力残留 |
| 导入/导出 | 导出 JSON，刷新后导入；取消导入覆盖 | 通过 | 导出文件可解析；导入需确认，取消不覆盖 |

## 本轮追加实测（2026-08-10）

| 范围 | 真实执行 | 结果 |
|---|---|---|
| 目标基础 | 浏览器填写目标、考试日期、每周 14 小时、每日 2 小时、上午高效时段并保存 | 通过；`localStorage` 中保留 `dailyHours` 与其余字段 |
| 卡住语义 | 创建“资料分析”任务，选择“卡住”，不预览直接确认，再查看预览后选择“保留原计划”确认 | 通过；未预览不改状态；确认后状态为“已卡住”，日期保持不变，新增 `confirmed-replan` 版本 |
| 结构化导入 | 代码级严格校验覆盖目标、科目 ID、资料主资料唯一性、任务状态/优先级/时长/日期、隐私设置 | 通过；非匹配结构显示导入失败且不覆盖现有状态 |
| 响应式溢出 | 浏览器页面在 320、375、390、768、1440 CSS 宽度读取 `scrollWidth - clientWidth` | 通过；五个宽度均为 0 |
| JS/静态门禁 | `python3 test_release.py`；提取脚本后 `node --check` | 通过 |
| 本轮浏览器状态流 | 本地 Chromium 创建“国考”、行测主资料与“资料分析”任务；选择“卡住”→“知识点不会”→影响预览→确认 | 通过；确认前任务为 `todo` 且无版本，确认后为 `blocked` 并写入事件和版本；预览明确显示目标节点影响与资料不变 |
| 首次建计划重构 | 空数据打开“今天”→“开始建计划”→选择国考→填写可用时间→保存 | 通过；进入今日页，生成 6 个国考科目和 6 份各科目的待替换主资料，今日页明确提示下一步是管理资料并添加首项任务 |
| 资料关联可用性 | 在“资料与设置”打开“关联科目”与任务科目选择器 | 通过；国考的 6 个预置科目均可选择，所有预置资料提供“改名”入口 |
| 页面结构 | 空计划、建计划、资料与设置三页导航 | 通过；首次打开落在“今天”，建计划为单一分步入口，日常页面不再直接呈现配置表单 |

## 视口检查

使用 Chromium DevTools 设备宽度检查首页和目标、资料、任务、调整对话框：

| 宽度 | 结果 |
|---:|---|
| 320px | 通过：单列、按钮全宽、字段换行，无横向滚动 |
| 375px | 通过：单列、卡片与底部操作无重叠 |
| 390px | 通过：单列、文案可换行 |
| 768px | 通过：单列断点、可读层级保持 |
| 1440px | 通过：双栏、最大宽度 1100px，长文本受控 |

## 技术静态检查

- `index.html`：无外部 CDN、无构建依赖；`localStorage` 键为 `learning-planner-release-v1`。
- `data-model.json`：JSON 语法校验通过。

## 未通过 / 待补充

1. AT-18 的真实触控设备测试尚未做，仅完成浏览器响应式检查与脚本读取溢出验证。
2. 发布门槛中的 5 名目标用户 AT-01/07/10/12 走查尚未完成，属于发布前阻塞。
3. 无障碍屏幕阅读器、键盘全流程、多浏览器回归尚未完成。
4. 未执行真实用户测试、真实触控设备测试；本轮不声称这些验证已通过。
# 2026-08-10 product-flow regression

- 真实浏览器首建流程：空白状态 -> 建计划 -> 国考 -> 填写每周 20 小时 / 每日 3 小时 -> 生成 6 个科目与 6 份起步资料。
- 真实浏览器任务流程：资料与设置 -> 填写任务名称、完成标准、时长 -> 加入今天 -> 自动回到今天页。
- 首页去重：同一条任务只在“现在做”主卡出现，今日清单显示其余任务或明确提示“上面的任务就是今天的全部安排”。
- 结构检查：旧 `screenLegacyPlan` 在运行时移除，重复 ID 重命名补丁已删除。
- 布局检查：`780px` 与 `390px` 宽度 `scrollWidth - clientWidth = 0`；390px 显示移动端底部导航。
- 代码门禁：`test_release.py`、`test_starter.py`、`node --check` 均通过。

边界：以上是本地浏览器回归，不替代真实触控设备、屏幕阅读器、完整键盘流程和多浏览器验收。

# 2026-08-11 quick-flow regression

- 简化首建流程：目标模板 -> 目标 -> 每天可用时间 -> 开始使用；日期、每周时长和高效时段折叠为可选项。
- 快速任务流程：创建考研起步结构 -> 首页输入“背 20 个单词” -> 加入今天；浏览器验证任务成为“现在做”，默认时长为 30 分钟，自动关联第一个可用科目和主资料。
- 移动布局：390px 宽度读取 `scrollWidth - clientWidth = 0`；快速输入区和时长按钮切为单列，没有横向溢出。
- 浏览器控制台：上述流程未发现 JavaScript error。

边界：本次仍仅为 Chromium 本地浏览器回归；未完成真实手机触控、屏幕阅读器、完整键盘流程、5 名目标用户走查或多浏览器验收。
